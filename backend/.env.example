APP_NAME=Jarvis Backend
ALLOWED_ORIGINS=http://127.0.0.1:5500,http://localhost:5500,null
GEMINI_MODEL=gemini-1.5-flash
NEWS_REGION=US:en
REQUEST_TIMEOUT=10

# Gemini API key is intentionally not stored here.
# Paste it into the frontend popup; the browser sends it to /api/gemini/generate.
